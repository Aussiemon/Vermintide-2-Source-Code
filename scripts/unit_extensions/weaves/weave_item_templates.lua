-- chunkname: @scripts/unit_extensions/weaves/weave_item_templates.lua

WeaveItemTemplates = {
	target_hit = {
		init = function (self)
			return
		end,
		server_update = function (self, dt, t)
			return
		end,
		client_update = function (self, dt, t)
			return
		end,
		destroy = function (self)
			return
		end,
	},
	destroy = {
		init = function (self)
			return
		end,
		server_update = function (self, dt, t)
			return
		end,
		client_update = function (self, dt, t)
			return
		end,
		destroy = function (self)
			return
		end,
	},
}
